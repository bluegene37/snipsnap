name: snipsnap
description: "Screen capture, annotation, and OCR desktop app for Windows, macOS, and Linux."
publish_to: 'none'

# Single source of truth for the app version. When bumping, also update
# msix_config.msix_version below (four segments: <version>.0).
version: 1.0.4+3

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.6
  path: ^1.9.1
  file_picker: ^12.0.0
  google_fonts: ^8.2.1
  uuid: ^4.6.0
  image: ^4.9.1
  hotkey_manager: ^0.2.3
  drift: ^2.34.3
  # Direct use in SandboxMigration.repairCapturePaths, which rewrites
  # capture paths inside the copied database before drift ever opens it.
  sqlite3: ^3.5.2
  sqlite3_flutter_libs: ^0.6.0+eol
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Used only by tool/qa_driver_main.dart (the /qa live-drive harness).
  flutter_driver:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.5
  build_runner: ^2.15.1
  dmg: ^0.1.10
  flutter_launcher_icons: ^0.14.4
  msix: ^3.16.7
  inno_bundle: ^0.11.2

# Inno Setup Windows installer configuration (dart run inno_bundle:build).
inno_bundle:
  id: 6b7e88bf-76f5-46aa-b2b9-e152504b86bb
  name: SnipSnap
  publisher: genexis.dev
  installer_icon: windows/runner/resources/app_icon.ico
  admin: auto
  languages:
    - english

# Regenerate platform icons with `dart run flutter_launcher_icons` after
# changing assets/icon/app_icon.png (1024x1024, transparent rounded corners,
# processed from assets/images/app_logo.png).
flutter_launcher_icons:
  image_path: "assets/icon/app_icon.png"
  windows:
    generate: true
    image_path: "assets/icon/app_icon.png"
    icon_size: 256
  macos:
    generate: true
    image_path: "assets/icon/app_icon.png"

# MSIX package for store/enterprise deployment. Build with `dart run msix:create`
# after `flutter build windows --release`. msix_version must be bumped in lock
# step with `version:` above.
msix_config:
  display_name: SnipSnap
  publisher_display_name: genexis.dev
  identity_name: dev.genexis.snipsnap
  msix_version: 1.0.4.0
  logo_path: windows/runner/resources/app_icon.ico

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/snipsnap_logo.png
    - assets/images/app_logo.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
